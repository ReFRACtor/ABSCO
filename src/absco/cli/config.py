"""``absco-config`` — config-assistant CLI (implemented in a later plan item)."""


def main():
    raise SystemExit(
        "absco-config is not implemented yet (see docs/packaging_plan.md, item 5)."
    )


if __name__ == "__main__":
    main()
