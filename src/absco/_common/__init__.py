"""Vendored utilities from the AER-RC/common repository.

Only the modules required by the ABSCO pipeline are vendored here so that the
installed package does not depend on the ``common`` git submodule at runtime.
"""
